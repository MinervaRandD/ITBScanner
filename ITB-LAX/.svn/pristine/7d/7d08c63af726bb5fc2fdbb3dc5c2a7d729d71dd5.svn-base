using Asi.Itb.Bll.Entities;
using Microsoft.VisualStudio.TestTools.UnitTesting;
namespace Bll.Test
{
    
    
    /// <summary>
    ///This is a test class for GpsPositionTest and is intended
    ///to contain all GpsPositionTest Unit Tests
    ///</summary>
    [TestClass()]
    public class GpsPositionTest
    {


        private TestContext testContextInstance;

        /// <summary>
        ///Gets or sets the test context which provides
        ///information about and functionality for the current test run.
        ///</summary>
        public TestContext TestContext
        {
            get
            {
                return testContextInstance;
            }
            set
            {
                testContextInstance = value;
            }
        }

        #region Additional test attributes
        // 
        //You can use the following additional attributes as you write your tests:
        //
        //Use ClassInitialize to run code before running the first test in the class
        //[ClassInitialize()]
        //public static void MyClassInitialize(TestContext testContext)
        //{
        //}
        //
        //Use ClassCleanup to run code after all tests in a class have run
        //[ClassCleanup()]
        //public static void MyClassCleanup()
        //{
        //}
        //
        //Use TestInitialize to run code before running each test
        //[TestInitialize()]
        //public void MyTestInitialize()
        //{
        //}
        //
        //Use TestCleanup to run code after each test has run
        //[TestCleanup()]
        //public void MyTestCleanup()
        //{
        //}
        //
        #endregion


        /// <summary>
        ///A test for Longitude
        ///</summary>
        [TestMethod()]
        public void LongitudeTest()
        {
            double latitude = 0F; // TODO: Initialize to an appropriate value
            double longitude = 0F; // TODO: Initialize to an appropriate value
            GpsPosition target = new GpsPosition(latitude, longitude); // TODO: Initialize to an appropriate value
            double expected = 0F; // TODO: Initialize to an appropriate value
            double actual;
            target.Longitude = expected;
            actual = target.Longitude;
            Assert.AreEqual(expected, actual);
            Assert.Inconclusive("Verify the correctness of this test method.");
        }

        /// <summary>
        ///A test for Latitude
        ///</summary>
        [TestMethod()]
        public void LatitudeTest()
        {
            double latitude = 0F; // TODO: Initialize to an appropriate value
            double longitude = 0F; // TODO: Initialize to an appropriate value
            GpsPosition target = new GpsPosition(latitude, longitude); // TODO: Initialize to an appropriate value
            double expected = 0F; // TODO: Initialize to an appropriate value
            double actual;
            target.Latitude = expected;
            actual = target.Latitude;
            Assert.AreEqual(expected, actual);
            Assert.Inconclusive("Verify the correctness of this test method.");
        }

        /// <summary>
        ///A test for GpsPosition Constructor
        ///</summary>
        [TestMethod()]
        public void GpsPositionConstructorTest()
        {
            double latitude = 0F; // TODO: Initialize to an appropriate value
            double longitude = 0F; // TODO: Initialize to an appropriate value
            GpsPosition target = new GpsPosition(latitude, longitude);
            Assert.Inconclusive("TODO: Implement code to verify target");
        }
    }
}
